import styled from 'styled-components';

const OpenCardFormStyles = styled.section`
  // border: solid 1px black;
  margin: 0 0 0 0;
  padding: 8px 6px 5px 8px;
  :hover {
    background-color: rgba(0.1, 0.1, 0.1, 0.2);
  }
  .hide {
    display: none;
  }
`;

export default OpenCardFormStyles;
