import styled from 'styled-components';

const EditTitleStyles = styled.section`
  input {
    font-family: 'Bitter', serif;
    font-weight: bolder;
    outline: none;
    border: solid 2px #0074d9;
    border-radius: 3px;
    padding: 0;
    padding: var(--padding-titles);
    font-size: 0.9rem;
  }
`;

export default EditTitleStyles;
